[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"
  # Trigger redeploy to pick up XANO_API_KEY environment variable

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
