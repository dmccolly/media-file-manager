{
  "name": "media-file-manager-heroku",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "build": "cd src && npm install && npm run build && cd .. && cp -r src/build/* build/",
    "heroku-postbuild": "mkdir -p build && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
