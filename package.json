{
  "name": "media-file-manager-heroku",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "build": "cd src && npm install && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
