{
  "name": "media-file-manager-heroku",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Skipping React build - using pre-built artifacts'",
    "heroku-postbuild": "echo 'Skipping heroku-postbuild - using committed build artifacts'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
  }
}
