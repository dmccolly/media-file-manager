{
  "name": "hoibf-file-manager-server",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm ci --prefix src && npm run build --prefix src && rm -rf build && cp -r src/build ./build"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  }
}
