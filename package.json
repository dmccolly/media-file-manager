{
  "name": "media-file-manager-heroku",
  "private": true,
  "engines": { "node": "18.x", "npm": "9.x" },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "bash -lc 'cd src && NPM_CONFIG_PRODUCTION=false npm ci && npm run build && cd .. && rm -rf build && mkdir -p build && cp -r src/build/* build/'"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  }
}
